# --- 核心运行依赖 ---
# PySide6 是 Qt 的官方 Python 绑定，负责所有的 GUI 界面和交互
PySide6 >= 6.0.0

# graphviz 是 Python 包装库，用于在代码中调用 dot 引擎生成图像
graphviz >= 0.20

# --- 开发与打包依赖 (可选) ---
# pyinstaller 用于将脚本封装成 Windows 下可执行的 .exe 文件
pyinstaller >= 5.0.0